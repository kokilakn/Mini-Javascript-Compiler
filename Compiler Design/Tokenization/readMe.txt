Commands to execute: 
	1) lex lexical_analysis.l
	2) make -f makefile 
	3) ./review1 ./INPUT/filename.txt

INPUT folder:
	1) input1.txt --> basic example, with scope increment within loop, correct execution.
	2) input2.txt --> correct do while loop example. Should execute without errors. 
	3) input3.txt --> empty do loop. Should return error and not continue parsing.
	4) input4.txt --> example showing semicolon missing error but still parsing continued showing error. Remove the line in grammar and show if the rule isnt included to continue parsing if no semicolon, then parsing stops when there is a line with no semicolon.
	5) input5.txt --> incorrect progrm. For not accepted by our program. Error.







